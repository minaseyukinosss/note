# Week3 过程记录：LangGraph 与可交付 Demo

> 本文件由原 Day 15-16 学习笔记、Day 15-16 复盘、Day 17-19 真工具与记忆、Day 20-21 Demo 交付指南整理而来。稳定概念已沉淀到 `02-概念详解.md`，实验与踩坑已回流到 `03-实验与踩坑.md`。

## Day 15-16：用 LangGraph 重写 ReAct

### 学习目标

- 用 StateGraph 重写 Week2 的手写 ReAct Agent。
- 对照理解 state、node、edge、conditional edge、END。

### 关键观察

- `state` 对应手写版的 `messages`，但 LangGraph 用 reducer 管理消息追加。
- `node` 对应一次操作，例如调用模型或执行工具。
- `edge` 对应下一步控制流，条件边相当于手写版的 `if msg.tool_calls`。
- `ToolNode` 替代了手写 dispatch，但工具描述、参数 schema 和错误处理仍然重要。

### 复盘结论

LangGraph 改善的是工程组织和可恢复能力，不会让同一个模型天然更会推理。Week2 的模型不确定性，如跳过工具、乱选工具、过早总结，在 Week3 仍然需要通过 prompt、工具设计、eval 和 trace 管理。

## Day 17-19：真工具与 Checkpointer

### 学习目标

- 跑通 `checkpoint_demo.py`，观察同一 `thread_id` 下的续聊。
- 跑通 `checkpoint_amnesia_demo.py`，验证换 `thread_id` 后状态隔离。
- 接入 Tavily 或本地文件读取这类真工具。

### 关键观察

- Checkpointer 保存的是图状态快照，不是模型内部长期记忆。
- `thread_id` 是状态隔离的关键，同一个 thread 可以续聊，不同 thread 不应串线。
- 真工具会带来真实失败模式：key 缺失、网络失败、工具返回为空、工具结果不可信。
- 搜索或文件读取的结果只能当数据，不能当系统指令。

### 复盘结论

Week3 开始进入工程化阶段：除了“能回答”，还要关心状态恢复、工具边界、失败可观测和 trace。Agent 的复杂度随着工具真实化明显上升。

## Day 20-21：Demo 交付

### 学习目标

- 选择一个足够小的 Demo 场景。
- 准备 README、示例对话、失败用例和 trace。
- 用交付物倒逼学习闭环。

### 选题原则

- 场景要窄，工具要少，验收要明确。
- 优先选“每日科技新闻摘要 Agent”“个人记账助手”“简易代码 Reviewer”这类能演示完整链路的小项目。
- 不追求多 Agent 或复杂规划，先把单 Agent 的工具、状态、trace、eval 做扎实。

### 复盘结论

三周结束时，目标不是“掌握所有 Agent 框架”，而是能解释并实现一条完整链路：用户目标进入 state，模型决定工具，工具结果写回，图继续执行，最终答案可追踪、可复盘、可评估。

## 回流位置

- 核心概念：见 `02-概念详解.md`
- 实验与报错：见 `03-实验与踩坑.md`
- 固定回归：见 `../../05-评估用例.md`
- 周级验收：见 `05-本周总结.md`

