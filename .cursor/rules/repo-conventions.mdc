---
description: 笔记仓库的全局约定（目录结构、命名、提交风格）
alwaysApply: true
---

# 仓库定位

这是一个**个人学习笔记仓库**，不是产品代码。优先级：**可读性 > 完整性 > 性能**。

# 目录结构（不要破坏）

```
<topic>/                       # 主题，如 javascript/、webpack/
├── README.md                  # 主题索引
└── <subtopic-or-weekX>/
    ├── README.md              # 子主题入口：目标 + 笔记索引 + 代码跑法
    ├── notes/                 # 所有 markdown 笔记放这里
    └── code/                  # 可运行的最小示例工程（可选）
```

- 含可运行代码的实践项目，**笔记归 `notes/`，源码归 `code/`**，不要混放。
- 纯笔记主题（如 `javascript/`）可以平铺 `.md` 文件，无需 `notes/code` 拆分。
- `ai-agent/` 有额外编号化约定，见 [ai-agent-learning.mdc](./ai-agent-learning.mdc)。

# 命名规范

- 笔记文件：中文标题或英文短横线，如 `闭包.md`、`week3-day1-notes.md`。
- 不要使用空格、大写、特殊符号。
- README 一律 `README.md`（大写）。
- 已采用编号体系的主题（如 `ai-agent/`）必须保持编号稳定，不要新增未编号的根文档或零散 day notes。

# 禁止提交

- `node_modules/`、`dist/`、`build/`、`*.lock`、`.DS_Store`
- `.env`、`.env.*`（除 `.env.example`）、`.venv/`、`__pycache__/`、`checkpoints.db*`、`*.sqlite*`
- 已在 `.gitignore` 中配置，发现遗漏请补充而不是删除规则。

# 新建主题/笔记时

1. 必须在父级 `README.md` 中**追加导航链接**，保持目录可发现。
2. 若新建主题，需创建对应 `README.md` 作为索引。
3. 若复制/重组 webpack 类工程，沿用 `notes/ + code/` 双子目录约定。
4. `notes/` 下笔记须**自洽可读**（复习时不假设读者记得第几周）：见 [notes-self-contained.mdc](./notes-self-contained.mdc)。

# 语言

- 笔记内容**统一使用中文**（代码、命令、专有名词保留英文）。
- 与用户对话也使用中文。
