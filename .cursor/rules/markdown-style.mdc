---
description: 笔记 markdown 的写作与排版风格
globs: **/*.md
alwaysApply: false
---

# 标题层级

- 单个文件必须只有**一个 H1**（`#`），作为标题。
- 章节用 H2（`##`）起步，逐级递增，不要跳级。

# 代码块

- 必须标注语言：`` ```js ``、`` ```bash ``、`` ```ts ``、`` ```json ``。
- 命令行示例统一用 `bash`，不要用 `shell` / `sh`。
- 长输出可截断并用 `// ...` 或 `# ...` 注明省略。

# 中英文混排

- 中英文之间**保留一个空格**，例：`使用 webpack 构建 ESM 模块`。
- 中文与数字之间也加空格：`耗时 3 秒`。
- 中文标点用全角（，。：；？！），英文标点用半角并加空格。

# 链接

- 相对路径优先，不要写绝对路径或 `file://`。
- 跨目录引用同仓库内容用相对路径，例：`[Week1 笔记](../week1-minimal/notes/day1-learning-notes.md)`。

# 列表与引用

- 无序列表统一用 `-`，不要用 `*` 或 `+`。
- 有序列表正常用 `1. 2. 3.`。
- 重要提示用 `> 💡` / `> ⚠️` / `> 📝` 引用块（仅在确有必要时）。

# 笔记结构建议

每篇笔记建议至少包含：

```markdown
# <标题>

## 背景 / 学习目标

## 核心概念

## 示例代码

## 易错点 / 易混淆点

## 小结
```

按需增减小节，**不要为了凑结构而堆砌内容**。
