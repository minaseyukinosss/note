# 阶段三 学习手册：工程能力

> 学习时只打开这个文件。本阶段以"搭一个标准工程"为主线，边搭边学。

## Day 46-50：项目结构 + uv

### 学习目标

- 理解 `src/` 布局、包 vs 模块、`__main__.py` 入口。
- 掌握 `uv init / add / run / sync`，读懂 `pyproject.toml` 和 `uv.lock`。

### 关键观察

- `uv add x` 和 `pip install x` 的区别？（自动写进 pyproject + 锁定）
- 为什么用 `src/` 布局而不是把包放根目录？（避免"意外 import 到本地目录"）
- 依赖分组 `--dev` 是什么？为什么测试 / lint 工具要和运行时依赖分开？

### 关键结论（自己填）

- 

## Day 53-55：配置管理

### 学习目标

- 用 `pydantic-settings` 从环境变量 / `.env` 读配置，带类型校验和默认值。

### 关键观察

- 配置为什么不直接用 `os.getenv`？（缺类型、缺校验、散落各处）
- `.env` 为什么不能提交？`.env.example` 放什么？
- 分环境（dev/prod）怎么切？

### 关键结论（自己填）

- 

## Day 56-59：日志系统

### 学习目标

- `logging` 的 logger / handler / formatter / level 层级；`rich` 美化。

### 关键观察

- `print` 和 `logging` 的本质区别？（级别、可关闭、可路由、可结构化）
- 为什么用 `logging.getLogger(__name__)` 而不是 root logger？
- Agent 里日志该记什么？（工具调用、输入输出、耗时、错误）

### 关键结论（自己填）

- 

## Day 60-65：测试 + 代码质量

### 学习目标

- pytest：fixture、参数化 `@pytest.mark.parametrize`、`monkeypatch` mock。
- ruff（lint+format）、pyright（类型）、pre-commit。

### 关键观察

- fixture 相比 setUp 好在哪？（组合、作用域、依赖注入）
- 怎么 mock 掉一个真实的 LLM/网络调用，让测试离线可跑？
- ruff 一个工具就同时做了 lint、format、import 排序（替代 flake8 + black + isort）。

### 关键结论（自己填）

- 

## Day 66：综合实战 标准工程重构

### 任务

- `uv init` 建工程，把阶段二的 `mini_chain` 迁进 `src/`。
- 加 `config.py`（pydantic-settings）+ `logging.py`。
- 写 `tests/`，含一个 `monkeypatch` mock 的用例。
- 通过 ruff + pyright。

### 记录区

- 我今天搞懂了：
- 我还卡在：
