# 阶段二 学习手册：高级特性

> 学习时只打开这个文件。练习在 `../code/`，稳定结论回流到"关键结论"区。

## Day 22-28：OOP + 抽象接口

### 学习目标

- `self`、类属性 vs 实例属性、classmethod / staticmethod、继承、MRO、`super()`。
- `ABC` + `@abstractmethod` 与 `Protocol` 的区别与选择。

### 关键观察

- 类属性写在哪、实例属性写在哪？共享的坑（可变类属性）？
- `super().__init__()` 在多继承下按什么顺序找？（`Cls.__mro__`）
- `ABC`（名义子类型，要显式继承）vs `Protocol`（结构子类型，长得像就行）——框架为什么两种都用？

### 关键结论（自己填）

- 

## Day 29-35：装饰器 + 闭包 + 魔术方法

### 学习目标

- 函数装饰器 → 带参装饰器 → `functools.wraps`；理解装饰器本质是"返回函数的闭包"。
- `__call__`、`__repr__`、`__eq__`、`__iter__`。

### 关键观察

- `@retry(times=3)` 比 `@retry` 多了一层——为什么要三层函数？
- 不加 `functools.wraps` 会丢失什么（`__name__` / docstring）？
- `__call__` 让实例可调用，这和框架里 `agent(x)` 有什么关系？

### 关键结论（自己填）

- 

## Day 36-40：生成器 / 迭代器 / 上下文管理器

### 学习目标

- `yield`、`yield from`、迭代器协议、`itertools`。
- `with`、`__enter__/__exit__`、`contextlib.contextmanager`。

### 关键观察

- 生成器是惰性的——`gen = (x for x in big)` 何时才真正计算？
- streaming 场景：为什么用 `yield` 而不是先攒成 list 再返回？
- `@contextmanager` 里 `yield` 前后分别对应 `__enter__` / `__exit__` 的什么？

### 关键结论（自己填）

- 

## Day 41-44：dataclass + typing

### 学习目标

- `@dataclass`、`field`、`frozen`；泛型 `list[T]`、`Optional`、`Union`/`|`、`Callable`、`TypedDict`、`Literal`、`Protocol`。

### 关键观察

- `@dataclass` 自动生成了哪些方法（`__init__`/`__repr__`/`__eq__`）？
- dataclass 和 Pydantic BaseModel 的区别？（校验！dataclass 不校验）
- `Literal["user", "system"]` 怎么帮你把 message role 类型收窄？

### 关键结论（自己填）

- 

## Day 45：综合实战 mini_chain

### 任务

- 实现 `Runnable` 基类，重载 `__or__`，让 `a | b | c` 组合成顺序管道。
- 每个 step 是一个可调用对象（用 `__call__`）。
- 全量类型注解，通过 pyright。

### 记录区

- 我今天搞懂了：
- 我还卡在：
