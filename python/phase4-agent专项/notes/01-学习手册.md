# 阶段四 学习手册：AI Agent 专项

> 学习时只打开这个文件。本阶段边写真实技术栈边读源码。

## Day 67-71：async / asyncio

### 学习目标

- 协程、`await`、事件循环、`gather`、`create_task`、`wait_for` 超时、`Semaphore` 限流。

### 关键观察

- 只调 `foo()`（async 函数）不 `await` 会发生什么？（拿到 coroutine，不执行）
- `gather` 并发 vs 顺序 `await` 的耗时差异，实测一下。
- 在 async 函数里 `time.sleep(5)` 会怎样？（阻塞整个事件循环，要用 `asyncio.sleep`）
- `Semaphore(5)` 怎么限制同时最多 5 个并发请求？

### 关键结论（自己填）

- 

## Day 74-77：httpx + API 封装

### 学习目标

- `httpx.AsyncClient`、超时、流式 `stream`、tenacity 重试、错误分类。

### 关键观察

- 为什么复用一个 `AsyncClient` 而不是每次新建？（连接池）
- 哪些错误该重试（超时、5xx），哪些不该（4xx 参数错）？
- 流式响应怎么逐块读？（`async for chunk in resp.aiter_lines()`）

### 关键结论（自己填）

- 

## Day 78-83：JSON + Pydantic 数据模型

### 学习目标

- 容错解析 tool_calls；用 Pydantic 建模 Message/ToolCall/State；校验器、`Field`、序列化。

### 关键观察

- LLM 返回的 JSON 可能不合法，怎么容错解析？
- `model_validate(data)` vs 直接构造的区别？
- `@field_validator` 怎么在校验阶段规整数据？
- dataclass 和 Pydantic 的核心区别（校验）再确认一次。

### 关键结论（自己填）

- 

## Day 84-86：FastAPI

### 学习目标

- 路由、Pydantic 请求/响应模型、依赖注入 `Depends`、async 端点、SSE 流式。

### 关键观察

- FastAPI 怎么用 Pydantic 自动做请求校验和文档？
- `Depends` 依赖注入解决了什么问题？（复用逻辑、便于测试时替换）
- SSE / StreamingResponse 怎么把 LLM token 流式吐给前端？

### 关键结论（自己填）

- 

## Day 87-90：源码阅读 + 终极项目

### 任务

- 读 `tenacity` 和 `langchain-core` 的 `Runnable.__or__`，各写一篇 300 字源码笔记。
- 收尾终极项目：FastAPI + Pydantic + httpx 异步 Agent 服务，带日志和测试。

### 记录区

- 我今天搞懂了：
- 我还卡在：
- 终极项目自检：API 能跑 / 流式正常 / 有测试 / 通过 ruff+pyright
